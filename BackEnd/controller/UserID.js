require("dotenv").config();
const axios = require(`axios`);

const IsNewUser = async (req, res) => {};

const NewUser = async (req, res) => {
  try {
    const response = await axios.get("", true);
    res.json;
  } catch (e) {
    throw e;
  }
};
